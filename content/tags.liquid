<h1>Tags</h1>

<ul>
{% assign tags = collections | getKeys | filterTagList | sortAlphabetically %}
{% for tag in tags %}
	{% capture tagUrl %}/tags/{{ tag | slugify }}/{% endcapture %}
	<li><a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a></li>
{% endfor %}
</ul>
